<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp"
        android:background="@android:color/white">

        <!-- ========================= -->
        <!-- TÍTULO -->
        <!-- ========================= -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Detalles del Tour"
            android:textStyle="bold"
            android:textSize="18sp"
            android:textColor="#2B746C"
            android:layout_marginBottom="12dp"/>

        <!-- ========================= -->
        <!-- HORA -->
        <!-- ========================= -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="@drawable/box_bg"
            android:padding="12dp"
            android:layout_marginBottom="8dp">

            <TextView
                android:text="Hora de inicio:"
                android:textStyle="bold"
                android:textColor="#2B746C"/>

            <TextView
                android:id="@+id/tvHoraInicio"
                android:text="8:00 AM"
                android:textColor="#2B746C"/>
        </LinearLayout>

        <!-- ========================= -->
        <!-- IDIOMAS -->
        <!-- ========================= -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="@drawable/box_bg"
            android:padding="12dp"
            android:layout_marginBottom="8dp">

            <TextView
                android:text="Idiomas ofrecidos:"
                android:textStyle="bold"
                android:textColor="#2B746C"/>

            <TextView
                android:id="@+id/tvIdiomas"
                android:text="Español - Inglés"
                android:textColor="#2B746C"/>
        </LinearLayout>

        <!-- ========================= -->
        <!-- FECHA -->
        <!-- ========================= -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="@drawable/box_bg"
            android:padding="12dp"
            android:layout_marginBottom="8dp">

            <TextView
                android:text="Fecha del Tour:"
                android:textStyle="bold"
                android:textColor="#2B746C"/>

            <TextView
                android:id="@+id/tvFechaTour"
                android:text="Marzo 15, 2025"
                android:textColor="#2B746C"/>
        </LinearLayout>

        <!-- ========================= -->
        <!-- RUTA / MAPA -->
        <!-- ========================= -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="@drawable/box_bg"
            android:padding="12dp"
            android:layout_marginBottom="8dp">

            <TextView
                android:text="Ruta:"
                android:textStyle="bold"
                android:textColor="#2B746C"
                android:textSize="16sp"
                android:layout_marginBottom="8dp"/>

            <!-- MAPA (por ahora imagen estática) -->
            <ImageView
                android:id="@+id/imgMapaRuta"
                android:layout_width="match_parent"
                android:layout_height="180dp"
                android:scaleType="centerCrop"
                android:src="@drawable/ruta"
                android:layout_marginBottom="8dp"/>

            <!-- CONTENEDOR DE UBICACIONES (opcional dinámico luego) -->
            <LinearLayout
                android:id="@+id/containerRuta"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"/>
        </LinearLayout>

        <!-- ========================= -->
        <!-- SERVICIOS EXTRA (DINÁMICOS) -->
        <!-- ========================= -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="@drawable/box_bg"
            android:padding="12dp"
            android:layout_marginBottom="12dp">

            <TextView
                android:text="Servicios extra"
                android:textStyle="bold"
                android:textSize="16sp"
                android:textColor="#2B746C"
                android:layout_marginBottom="8dp"/>

            <!-- 🔥 AQUÍ SE AGREGAN LOS EXTRAS DESDE JAVA -->
            <LinearLayout
                android:id="@+id/containerExtras"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"/>

            <!-- BOTONES -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center"
                android:layout_marginTop="12dp">

                <Button
                    android:id="@+id/btnAgregarExtras"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:layout_marginEnd="6dp"
                    android:text="Agregar al Tour"
                    android:backgroundTint="#4CAF50"
                    android:textColor="@android:color/white"/>

                <Button
                    android:id="@+id/btnSalir"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:layout_marginStart="6dp"
                    android:text="Salir"
                    android:backgroundTint="#F44336"
                    android:textColor="@android:color/white"/>
            </LinearLayout>

        </LinearLayout>

    </LinearLayout>
</ScrollView>
