<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_superadmin"
    tools:context=".superadmin.IdiomasActivity">

    <!-- ✅ TOOLBAR -->
    <include
        layout="@layout/top_app_bar_superadmin" />

    <!-- ✅ CONTENIDO PRINCIPAL -->
    <LinearLayout
        android:id="@+id/contentContainer"
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="?attr/actionBarSize"
        android:layout_marginBottom="70dp"
        android:padding="16dp"
        android:gravity="center_horizontal">

        <!-- ✅ Flecha + Título -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingBottom="8dp">

            <ImageView
                android:id="@+id/btnBack"
                android:layout_width="26dp"
                android:layout_height="26dp"
                android:src="@drawable/ic_arrow_back"
                android:tint="#009688"
                android:padding="4dp"
                android:layout_marginEnd="8dp" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Idiomas más hablados"
                android:textColor="#009688"
                android:textSize="20sp"
                android:textStyle="bold" />
        </LinearLayout>

        <!-- ✅ FECHA -->


        <!-- ✅ GRÁFICO DE DONA -->
        <com.github.mikephil.charting.charts.PieChart
            android:id="@+id/pieChartIdiomas"
            android:layout_width="300dp"
            android:layout_height="300dp"/>

    </LinearLayout>

    <!-- ✅ FOOTER (AHORA SÍ QUEDA ABAJO SIEMPRE) -->
    <include
        android:id="@+id/bottomBar"
        layout="@layout/navigation_bottombar_superadmin"
        android:layout_gravity="bottom" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
