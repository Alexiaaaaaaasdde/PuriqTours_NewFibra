<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.PuriqTours" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/teal_dark</item>
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:navigationBarColor">@color/black</item>
    </style>

    <!-- Estilo específico para el toolbar verde -->
    <style name="Widget.PuriqTours.Toolbar" parent="Widget.MaterialComponents.Toolbar.Primary">
        <item name="android:background">#26A69A</item>
        <item name="android:theme">@style/ThemeOverlay.MaterialComponents.Dark.ActionBar</item>
        <item name="titleTextColor">@android:color/white</item>
    </style>
    <style name="MyToolbarTheme" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar" />

</resources>