<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme -->
    <style name="Base.Theme.PuriqTours" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Colores principales -->
        <item name="colorPrimary">@color/teal_700</item>
        <item name="colorSecondary">@color/teal_700</item>
        <item name="colorOnPrimary">#2B746C</item>

        <!-- 🔹 Barra de estado blanca -->
        <item name="android:statusBarColor">#2B746C</item>
        <!-- true = iconos oscuros (negros) para que se vean sobre el blanco -->
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.PuriqTours" parent="Base.Theme.PuriqTours" />

</resources>
